# Disable Automount

```cmd
mountvol /n     # disable automount
mountvol /n     # to remove drive letters for previously connected drives
mountvol /e     # enable automount
# chkdsk K: /f /r /x # Check disk on K: to control blocks and errors (before check dismounts drive)
```
